{% extends 'ApplicationSonataUserBundle:Profile:base.html.twig' %}

{% block content %}
<form action="{{ path('user_edit_preferences') }}" method="post">
	{{ form_widget(form) }}

	<div class="form-actions">
		<button class="btn btn-primary" name="submit" type="submit">{{ 'user.preferences.submit'|trans }}</button>
	</div>
</form>
{% endblock %}

