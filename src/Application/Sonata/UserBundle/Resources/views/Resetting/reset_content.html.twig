<form action="{{ path('fos_user_resetting_reset', { token: token }) }}" class="form-centered" method="post">
    {{ form_widget(form) }}

	<input class="btn btn-primary" type="submit" value="{{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}" />
</form>

