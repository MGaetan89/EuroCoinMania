{% import "EuroCoinBundle::macros.html.twig" as macros %}
<!DOCTYPE html>
<html>
<head>
	<link href="{{ asset('bundles/eurocoin/css/bootstrap.css') }}" rel="stylesheet" />
	<link href="{{ asset('bundles/eurocoin/css/style.css') }}" rel="stylesheet" />
	<link href="{{ asset('bundles/eurocoin/css/bootstrap-responsive.css') }}" rel="stylesheet" />

	<meta charset="UTF-8" />

	<script src="{{ asset('bundles/eurocoin/js/jquery.js') }}" type="text/javascript"></script>
	<script src="{{ asset('bundles/eurocoin/js/bootstrap.js') }}" type="text/javascript"></script>

	<title>{% block title %}{{ 'sitename'|trans }}{% endblock %}</title>
</head>
<body>
{% set route = app.request.get('_route') %}
<div class="navbar navbar-fixed-top">
	<div class="navbar-inner">
		<div class="container">
			<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</a>

			<a class="brand" href="{{ path('EuroCoinBundle_homepage') }}">{{ 'sitename'|trans }}</a>

			<div class="nav-collapse">
				<ul class="nav">
					<li class="{% if route|slice(0, 7) == 'country' %}active {% endif %}dropdown">
						<a href="{{ path('country') }}" class="dropdown-toggle" data-toggle="dropdown" data-target="#">{{ 'countries'|trans }}<b class="caret"></b></a>
						<ul class="dropdown-menu">
							<li{% if route == 'country' %} class="active"{% endif %}><a href="{{ path('country') }}"><i class="icon-list-alt"></i> {{ 'country.list'|trans }}</a></li>
							<li{% if route == 'country_new' %} class="active"{% endif %}><a href="{{ path('country_new') }}"><i class="icon-plus"></i> {{ 'country.add'|trans }}</a></li>
							<li class="divider"></li>
							{% for country in euro_countries %}
							<li{% if route == 'country_show' and app.request.get('id') == country.id %} class="active"{% endif %}><a href="{{ path('country_show', { 'id': country.id }) }}">{{ country.name|trans }}</a></li>
							{% endfor %}
						</ul>
					</li>
					<li{% if route|slice(0, 4) == 'coin' %} class="active"{% endif %}><a href="{{ path('coin') }}">{{ 'coin.list'|trans }}</a></li>
					<li{% if route|slice(0, 5) == 'value' %} class="active"{% endif %}><a href="{{ path('value') }}">{{ 'value.list'|trans }}</a></li>
				</ul>

				{% if app.user %}
				<p class="navbar-text pull-right">
					Connecté en tant que <a href="{{ path('fos_user_profile_show') }}">{{ app.user }}</a> -
					<a href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
				</p>
				{% else %}
				<ul class="nav pull-right">
					<li><a href="{{ path('fos_user_registration_register') }}">Inscription</a></li>
					<li><a href="{{ path('fos_user_security_login') }}">Connexion</a></li>
				</ul>
				{% endif %}
			</div>
		</div>
	</div>
</div>

<div class="container">
	{% block body %}
		{% block fos_user_content %}{% endblock %}
	{% endblock %}
</div>

{% block javascript %}
{% endblock %}
</body>
</html>
