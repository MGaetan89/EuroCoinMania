<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST">
	{{ form_widget(form) }}

	<div class="form-actions">
		<input class="btn btn-primary" type="submit" value="{{ 'user.register_action'|trans }}" />

		<a class="btn" href="{{ path('fos_user_security_login') }}">{{ 'user.login'|trans }}</a>
	</div>
</form>
